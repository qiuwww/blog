/**
 * Solves equations of the form a * x = b
 * @example Example usage of method1. 例子的标题
 * // returns 2
 * method1(5, 10);
 * @example
 * // returns 3
 * method(5, 15);
 * @returns {Number} Returns the value of x for the equation.
 */
method1 = function (a, b) {
  return b / a;
};
