// array-extensions.js
cg("************array-extensions.js************")
{
/**
 * 1.Array.from方法用于将两类对象转为真正的数组：
 * 类似数组的对象（array-like object）和可遍历（iterable）的对象
 * （包括ES6新增的数据结构Set和Map）。用来替代 Array.slice.call()
 *
 *
 *
 * 
 */


}
cg("************array-extensions.js end************")